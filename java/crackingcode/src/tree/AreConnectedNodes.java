package tree;

public class AreConnectedNodes {

	/**
	 * add Visited bit in the node
	 */
	
	/**
	 * perform BFS on the source add only those nodes where visited is false.
	 * if destination is arrived return true
	 * else return false
	 */
}
