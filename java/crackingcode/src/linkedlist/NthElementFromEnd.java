package linkedlist;

public class NthElementFromEnd {

	/**
	 * CREATE A WINDOW OF K ELEMENTS AND MOVE IT ACROSS 
	 * THE ARRAY TILL THE END OF THE WINDOW REACHES THE END OF THE ARRAY
	 * 
	 * keep 
	 * 	head
	 * 	tail
	 *
	 *  for i = 0 to k
	 *   if(head == null) return null; array is shorter than k length
	 *   head = head.next;
	 *  end for
	 *  
	 *  
	 *  while(tail.next !=null)
	 *  head = head.next; // the head starts movign.
	 *  tail = tail.next; 
	 *  
	 *  return head;
	 */
	
	
	
}
