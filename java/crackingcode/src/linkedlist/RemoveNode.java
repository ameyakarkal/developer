package linkedlist;

public class RemoveNode {

	/**
	 * given reference to the only node that needs to remove
	 * ask ? double linked list
	 * 1. copy data from node.data = node.next.data
	 * 2. refer node.next = node.next.next
	 * 
	 * technically you delete the next node but copy the data form the next node.
	 * null check for node.next before start
	 */
}
